# Execution state of the active story-delivery workflow.
# Schema: docs/workflow/state-schema.md
# Writer: story-orchestrator ONLY. Stage Skills never write this file.
version: 1
story: US-001
workflow: story-delivery
current_stage: DESIGN_REVIEW
previous_stage: DB_DESIGN
last_completed_stage: DB_DESIGN
status: IN_PROGRESS
attempt: 2
last_invoked_skill: db-designer
last_result:
  verdict: PASS
  stage: DB_DESIGN
  recorded_at: 2026-09-02T20:44:41Z
last_artifacts:
  - type: database_design
    path: docs/designs/database/US-001-db-design.md
    version: 2
  - type: entity_model
    path: docs/designs/database/US-001-entity-model.md
    version: 1
pending_human_gate: null
blocking_issues: []
non_blocking_findings:
  - "HUMAN_SPEC_APPROVAL was approved on 2026-09-02 by human:KShust against specification v14 and review v11 (automated verdict PASS, 0 Critical, 0 Major, 4 Minor). The approval comment is recorded in full in docs/workflow/history.jsonl."
  - "FR-18 is decided, not tacit: the four JWT variables (JWT_ACCESS_SECRET, JWT_REFRESH_SECRET, JWT_ACCESS_TTL, JWT_REFRESH_TTL) are removed from .env.example. Nothing this Story ships reads them, and SC-3 keeps them out of src/config/env.ts until the Story that validates each. US-002 re-adds them together with the code that reads them. The item was carried to two consecutive gates; the second gate answered it."
  - "Review v11's four Minors are accepted as design inputs for API_DESIGN and IMPLEMENTATION_PLANNING, not as blockers. m-1 carries real downstream weight: the specification requires express.json()'s errors to be translated into PayloadTooLargeError and ValidationError but names no owner for that translation, while the 415's owner is named - API_DESIGN and the plan should settle which component performs it."
  - "Review v11's remaining three Minors, for the record: m-2 the revision-14 preamble note miscounts its own changes (names FR-21, which carried no decision id; omits BR-2, which did; wrong counts for edge cases and component rows); m-3 FR-5 closes with an obligation on US-003 that nothing in this Story can satisfy or test; m-4 FR-5 names the id field without stating its representation while citing AC-11 only for createdAt."
  - "Review independence is weaker at this gate than at the previous one, and the review artifact says so itself: specification v14 and review v11 were produced in the same session turn by the same model under different Skills. The PASS should not be read as an independent second opinion. Recorded at approval; it does not reopen the gate."
  - "history.jsonl gap, recorded not repaired: no SPEC_REVIEW -> SPECIFICATION CHANGES_REQUIRED event was ever appended for review v8, so SPECIFICATION's derived run count stays one lower than its real number of runs, and its attempt series carries a deliberate one-step jump at the rejection transition. history.jsonl is append-only, so the missing event was not invented."
  - "Timestamp defect, recorded not repaired: the history events written at SPECIFICATION attempts 3 and 4 carry 2026-09-02T09:12:00Z and 10:05:00Z, both ahead of the real clock, because those runs hardcoded a string instead of reading the system clock as artifact-schema.md requires. Whether to correct them is a human decision."
  - "Harness gap now due, named by the clarification report and deferred until US-001 cleared this gate: stage-map.yaml routes no loop_back to CLARIFICATION from a stage that discovers the need, while us-clarifier is the registry's only permitted writer - so registry revisions 3, 4, 5 and 7 were all written outside a stage run. The workflow has no path back into the file it defines as authoritative."
  - "spec-verifier SKILL.md contradicts itself: its result-envelope example shows artifact_status: APPROVED while its own report template writes status: DRAFT. Correcting the Skill file is a harness change and out of scope for US-001."
  - "API_DESIGN addressed review v11's m-1: docs/designs/api/US-001-api-design.md names the application-level middleware chain immediately after the JSON body parser as the owner of translating that parser's errors into PayloadTooLargeError and ValidationError, and rules out the centralized error middleware because AD-6 defines it as mapping only ZodError and DomainError. The filename remains IMPLEMENTATION_PLANNING's choice, as FR-22, FR-23 and FR-24 already leave theirs."
  - "API_DESIGN addressed review v11's m-4: the contract states the id field as string with format uuid, cited to AC-11 and PC-3. m-2 and m-3 are not addressable at this stage - both are defects in the Specification, an artifact API_DESIGN does not own, and correcting either means superseding a document that has passed its human gate."
  - "Error codes assigned by API_DESIGN and now part of the contract (AC-6 makes renaming one a breaking change): VALIDATION_FAILED (400), MALFORMED_JSON (400), PAYLOAD_TOO_LARGE (413), UNSUPPORTED_MEDIA_TYPE (415), RATE_LIMIT_EXCEEDED (429), INTERNAL_ERROR (500). EMAIL_ALREADY_REGISTERED (409) was already decided in AC-6 and was not reassigned. The five schema-failure cases share one code and are distinguished by details.fieldErrors, because AD-6 maps them all through a single ZodError branch with no throw site at which a per-case code could be supplied."
  - "Implementation consequence recorded by API_DESIGN, easy to miss and it would silently break VR-11: Zod reports an unrecognized body key as an issue whose path is the object root, so the default flattening leaves details.fieldErrors empty for the unknown-property 400. The contract keys that case by the offending property name, so the implementation must map the unrecognized-keys issue's own key list into fieldErrors rather than relying on the default helper."
  - "Left for IMPLEMENTATION_PLANNING, not blocking: express-rate-limit emits RateLimit-* headers by default and Retry-After on a 429. SC-3 decides the threshold and the mount point and says nothing about headers, and no requirement asks for them, so the contract declares none - declaring a response header the Specification does not require is what this stage's checklist forbids. The consequence is that the implementation emits headers the contract does not describe unless it disables them; either resolution is defensible and adding a declared header later is additive."
  - "DB_DESIGN finding, needs a human decision before IMPLEMENTATION: Prisma 7 requires a driver adapter on PrismaClient. In the installed @prisma/client 7.10.0 the PrismaClientOptions type is a union of exactly two shapes - one requiring accelerateUrl (Accelerate), one requiring adapter - and the type documents the adapter as required unless connecting through Accelerate. The official PostgreSQL adapter @prisma/adapter-pg and its pg driver are absent from package.json and node_modules (verified). That is a new dependency, which SC-6, SR-10 and the AGENTS.md Prohibited list make an explicit human decision. It changes nothing in the persistence model, so DB_DESIGN returned PASS, but src/lib/prisma.ts cannot construct a working client without it and US-001 cannot be implemented until it is approved. The implementation plan and HUMAN_PLAN_APPROVAL are the intended place."
  - "DB_DESIGN finding for IMPLEMENTATION_PLANNING: Prisma 7 no longer accepts url in the datasource block - url = env('DATABASE_URL') is validation error P1012 on 7.10.0, and prisma migrate status then reports that datasource.url is required in the Prisma config file (both verified by running the commands). US-001 must therefore add a prisma.config.ts carrying the migration connection URL, and that file has to read DATABASE_URL from the environment. AGENTS.md states flatly that process.env is read only in src/config/env.ts (AD-7), while the eslint rule enforcing it is scoped to files: [src/**/*.ts], so a root-level prisma.config.ts would pass npm run lint while contradicting the prose - the same position scripts/ already occupies. Either the config file reads the variable and AD-7 is narrowed to src/, or it imports the validated value; the plan should choose deliberately rather than let the lint silence decide."
  - "DB_DESIGN finding, recorded not specified: a database-level CHECK (email = lower(btrim(email))) would stop a future writer inserting an unnormalized address past the boundary, and PC-4 says a business invariant expressible as a database constraint is expressed as one. It is not in the design because no requirement asks for it and Prisma cannot express a CHECK in schema.prisma, so it would be raw SQL in the migration sitting outside what Prisma reconciles against. DESIGN_REVIEW can disagree with the judgement rather than have to notice the question."
  - "Recorded by DB_DESIGN for whoever takes the adapter decision: persistence-conventions.md PC-1 predates Prisma 7. What it says about DATABASE_URL and about one PrismaClient in src/lib/prisma.ts is still true; what it does not describe is that the client now needs an adapter object and that migrations read their URL from a separate config file. Amending a convention document is not a design stage decision, so the gap is recorded rather than closed."
  - "DESIGN_REVIEW v1 returned CHANGES_REQUIRED with loop_back key changes_required_api. The database design and entity model were accepted as they stand and MUST be carried forward unrevised - API_DESIGN is the only stage re-entered. Two Major findings, both in the API design, both dischargeable by adding a paragraph; the OpenAPI contract needs no structural change for either."
  - "d-1 (Major, API) - the 429 has no carrier and its default body violates AC-6. The contract declares RATE_LIMIT_EXCEEDED but no approved artifact gives any component the power to produce that body: express-rate-limit answers with its own default payload, and AD-6 defines the centralized error middleware as mapping only ZodError and DomainError. API_DESIGN must name what produces the 429 body. The review notes resolution (a) would need an amendment to architecture.md AD-6, which is a convention change and a human decision escalated at HUMAN_PLAN_APPROVAL; resolution (b) escalates nothing. This interacts with the undeclared RateLimit-* / Retry-After header question already recorded for IMPLEMENTATION_PLANNING, since both concern the limiter response."
  - "d-2 (Major, API) - a syntactically valid but non-object JSON body (an array, a string, a number) yields a 400 that no declared branch covers, and the FieldErrors schema sets no minProperties, so an empty fieldErrors validates against the contract while the prose says fieldErrors is always present and never empty. API_DESIGN must state which declared 400 branch covers this case and how its details is populated, and make schema and prose agree."
  - "d-3 (Minor, database) CORRECTS THE EARLIER ADAPTER ENTRY IN THIS LIST: the DB_DESIGN finding that @prisma/adapter-pg is an unapproved new dependency requiring a decision at HUMAN_PLAN_APPROVAL is stale. Commit 0339b4a (author KShust) adds @prisma/adapter-pg pinned to 7.10.0 and records the SC-6 approval in its message; the review verified the package is installed. IMPLEMENTATION_PLANNING must NOT carry the adapter to HUMAN_PLAN_APPROVAL as an open decision - it should cite 0339b4a as the approval. The design was deliberately not revised for this; the review is the artifact IMPACT_ANALYSIS and IMPLEMENTATION_PLANNING read alongside it. The design's two companion notes (prisma.config.ts and the AD-7 process.env question; PC-1 predating Prisma 7) stand unchanged and are still due."
  - "d-4 and d-5 (Minor, API), no correction required. d-4: RegisterRequest.email carries format: email, minLength: 1 and maxLength: 254 while BR-2/VR-4 normalize before validation, so the declared constraints describe the normalized value - recorded because AC-10 generates the contract from Zod, and a pipeline that transforms before validating may not render as a plain string with format and bounds, so the generated docs/api/openapi.json may not match this contract's shape for that field. IMPLEMENTATION_VERIFICATION should compare semantically. d-5: minLength: 1 on email is redundant under format: email; noted only so a later reader does not take it for a rule with a requirement behind it."

  - "API_DESIGN v2 discharged design review d-1 by recording a human decision rather than making one: commit fa21f62 (author KShust, 2026-09-02) amends architecture.md AD-6 to add TooManyRequestsError, extends the handler mapping with 429, and names US-001 as the Story that creates the class. The carrier is the rate limiter's own handler calling next(new TooManyRequestsError(...)), which keeps the error body constructed in the single place AD-6 makes responsible. NOTHING IS ESCALATED TO HUMAN_PLAN_APPROVAL for this - the design review's instruction to escalate was conditional on the AD-6 amendment still being needed, and it has been made. IMPLEMENTATION_PLANNING carries the class, not the question."
  - "New finding raised by API_DESIGN v2, for whoever revises the Specification: FR-21 states US-001 creates four DomainError subclasses and lists them; AD-6 after fa21f62 names five, TooManyRequestsError included. FR-21 restates AD-6 rather than deciding it, so the convention is canonical and the Specification is stale on this point. It is APPROVED and past its human gate and only spec-writer may revise it, so this is recorded, not repaired. Non-blocking: the authoritative class list is AD-6's and the contract declares the 429 the fifth class carries. The same consequence is recorded in fa21f62's own commit message."
  - "API_DESIGN v2 discharged design review d-2: a body that is valid JSON but not an object (a JSON array) is answered as VALIDATION_FAILED with details.fieldErrors naming email and password, both as not supplied - the same answer the contract already gives for a body of {} and for a bodyless POST. MALFORMED_JSON was rejected because the body parsed; an empty fieldErrors and a formErrors member were rejected as breaking VR-11 and widening AC-6. The contract gained minProperties: 1 on FieldErrors so the schema now enforces what the prose said. Verified on the installed libraries: body-parser 2.3.0 strict mode accepts only a first character of { or [, and zod 4.5.4 reports the array as one invalid_type issue at the object root."
  - "Implementation consequence recorded by API_DESIGN v2, the second of its kind and equally easy to miss: the default Zod flattening leaves details.fieldErrors empty for BOTH the unknown-property 400 and the non-object-body 400, because both issues carry a root path. The implementation must map the unrecognized-keys issue onto the offending property name AND the root-level invalid_type issue onto the two required field keys. minProperties: 1 is what makes either omission visible to a contract test rather than only to a careful reader; a Zod record does not emit that keyword on its own, so it must be supplied as .openapi() metadata or the generated document silently re-admits the response d-2 was raised about."
  - "HARNESS CHANGE made during this transition, approved by the user in-session on 2026-09-02: scripts/validate-harness.py check_input_versions now grades stale-input severity by where the stale artifact's owning stage sits relative to current_stage in stage_order. At or after current_stage the re-run is still ahead and it warns; before current_stage the workflow has moved past the stage that owed the re-run and it stays an ERROR. The rebuttal path (assessed_version + assessment) is unchanged and still fully checked. Reason: a loop-back makes every downstream artifact stale in the same instant, and neither remedy is reachable inside the turn that creates it - the orchestrator runs one stage per invocation and no Skill may write an artifact another Skill owns - so the previous rule failed every loop-back at the moment it succeeded, with no repair available to the turn being failed. docs/workflow/artifact-schema.md gained a When the re-run is still ahead section stating the same rule, so the document and the validator agree. The three US-001 stale-input findings are now warnings and validate:harness exits 0."

  - "DB_DESIGN re-ran at attempt 2 against API design v2 and returned PASS. The persistence model is unchanged - not carried forward by default, but re-read against v2 and found not to consume any of it: v2's two changes are both in the error model (the 429's TooManyRequestsError carrier, and the non-object-JSON-body case routed to VALIDATION_FAILED with minProperties: 1 on FieldErrors), a 429 never reaches a repository, the non-object body fails at the HTTP boundary, and RegisterRequest/RegisterResponse are byte-identical between v1 and v2. The judgement is recorded where it is auditable and reviewable: assessed_version: 2 plus a specific assessment on both API inputs of both DB artifacts, per artifact-schema.md. entity_model stays at version 1 because its content did not change; db_design moves to version 2 because of the correction below."
  - "DB_DESIGN v2 CORRECTED THE STALE ADAPTER CLAIM AT ITS SOURCE, which changes what IMPLEMENTATION_PLANNING has to do. Revision 1 of the database design stated that @prisma/adapter-pg and pg were absent from package.json and node_modules and that the new dependency needed a human decision at HUMAN_PLAN_APPROVAL. Design review v1 d-3 found that stale and addressed the correction to IMPLEMENTATION_PLANNING, on the stated assumption that DB_DESIGN would not re-run. It did re-run, and it owns the artifact, so the false statement is now fixed in the design itself rather than living only in a review that a later reader has to reconcile against it. Re-verified during the re-run: @prisma/adapter-pg 7.10.0 is declared in package.json and installed, pg 8.23.0 arrives transitively, and commit 0339b4a (author KShust, 2026-09-02) records the SC-6 approval and its reason. IMPLEMENTATION_PLANNING cites 0339b4a; it does not carry the adapter to the gate."
  - "The db-design's two other companion findings are unchanged and still due, both for IMPLEMENTATION_PLANNING: prisma.config.ts must read DATABASE_URL while AD-7 assigns process.env to src/config/env.ts alone and the lint rule enforcing it covers only src/**, so the resolution should be chosen deliberately rather than left to the lint's silence; and PC-1 predates Prisma 7 and describes neither the adapter object nor the separate migration config file, which is a convention amendment nobody has made. The third, the deliberately unspecified CHECK (email = lower(btrim(email))), is still offered to DESIGN_REVIEW to disagree with."
  - "Expected at DESIGN_REVIEW, not a defect: design review v1 now records stale inputs on two counts - api_design v1 (now v2) and database_design v1 (now v2). DESIGN_REVIEW is the stage the workflow has just entered, so both are warnings under the artifact-schema.md re-run-is-still-ahead rule and validate:harness exits 0. They clear when design-reviewer re-runs, which is the next transition. The re-run reviews API design v2 in full; the persistence design it already accepted changed only in the adapter paragraph its own d-3 asked for."

started_at: 2026-08-31T00:00:00Z
updated_at: 2026-09-02T20:44:41Z
completed_at: null
archived_at: null
